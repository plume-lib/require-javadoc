/** My record. */
record MyRecord(
    /** First field. */
    String first,
    String second,
    /** Third field. */
    String third) {}

record MyOtherRecord(
    /** First field. */
    String first,
    /** First field. */
    String second) {}

class Undocumented {}
