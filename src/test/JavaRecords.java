/** My record. */
record MyRecord(
    /** First field. */
    String first,
    String second,
    /** Third field. */
    String third) {}

record MyOtherRecord(
    /** First field. */
    String first,
    /** Second field. */
    String second) {}

class MyUndocumentedClass {}
